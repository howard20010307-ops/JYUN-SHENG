# 複製為 .env 後再填。Vite 只會暴露 VITE_ 開頭的變數。

# Firebase（選用）
# VITE_FIREBASE_API_KEY=
# VITE_FIREBASE_AUTH_DOMAIN=
# VITE_FIREBASE_PROJECT_ID=
# VITE_FIREBASE_STORAGE_BUCKET=
# VITE_FIREBASE_MESSAGING_SENDER_ID=
# VITE_FIREBASE_APP_ID=

# JSONBin 全自動雲端備份：在 https://jsonbin.io 註冊 → API Keys 複製 $MASTER_KEY →
# 以 POST /v3/b 建一筆空 Bin 或用網站建立，把 Bin id 與金鑰填在下方，重新 npm run dev。
# 注意：金鑰會被打進前端 bundle。建議用 Base64。PowerShell 內金鑰請用單引號包（雙引號內 $ 會被展開）：
# [Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes('在單引號內貼上整條金鑰'))
# 輸出貼到 VITE_JSONBIN_X_MASTER_KEY_B64（等號後不要加引號）。
# VITE_JSONBIN_BIN_ID=
# VITE_JSONBIN_X_MASTER_KEY_B64=
# VITE_JSONBIN_X_MASTER_KEY=
